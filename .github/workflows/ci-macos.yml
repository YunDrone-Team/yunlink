name: macOS CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Configure
        run: cmake --preset ci-ninja

      - name: Build
        run: python3 tools/build_fast.py --preset ci-ninja

      - name: Test
        run: ctest --test-dir build/ci-ninja --output-on-failure

  bindings:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Configure
        run: cmake --preset ci-ninja

      - name: Build Core Bindings Targets
        run: cmake --build --preset ci-ninja --target yunlink_ffi test_c_ffi_v2

      - name: Test FFI Contracts
        run: ctest --test-dir build/ci-ninja -R "test_c_ffi_v2" --output-on-failure

      - name: Test Rust Bindings
        run: |
          cargo test -p yunlink --manifest-path bindings/rust/Cargo.toml

      - name: Test Python Bindings
        shell: bash
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          python -m pip install -q --upgrade pip
          python -m pip install -q pytest -e bindings/python
          python -m pytest -q bindings/python/tests

      - name: Wheel Smoke
        shell: bash
        run: tools/bindings/build_python_wheel.sh
